using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class GroundedTestText : MonoBehaviour
{
/*   
[SerializeField]
    private TextMeshProUGUI _text;

    private void Update()
    {
        if (PlayerCharacterController.isGrounded == true)
        {
            this._text.text = "GROUNDED!";
            this._text.color = Color.green;
        }
        else
        {
            this._text.text = "NOT GROUNDED!";
            this._text.color = Color.red;
        }
    }
    */
}
